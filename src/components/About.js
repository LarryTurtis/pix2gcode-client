/* eslint-disable import/no-anonymous-default-export */

export default (props) => (
  <div className="about">
    This site exists for the sole purpose of uploading sprite sheets and
    converting them to 2D gcode files.
  </div>
);
